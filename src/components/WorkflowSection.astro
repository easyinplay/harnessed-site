---
const stages = [
  { num: '①', name: 'research', role: 'multi-source RAG', optional: true },
  { num: '②', name: 'discuss', role: '3-layer gates', optional: false },
  { num: '③', name: 'plan', role: 'task_plan.md', optional: false },
  { num: '④', name: 'task', role: 'clarify·code·test·deliver', optional: false },
  { num: '⑤', name: 'verify', role: '7 sub-checks', optional: false },
  { num: '⑥', name: 'retro', role: 'lessons captured', optional: false },
]
---

<section data-fade class="py-24 bg-[#0F0F15]">
  <div class="max-w-6xl mx-auto px-6">
    <h2 class="text-[32px] leading-[42px] font-medium text-white text-center">
      One command. Six stages. Zero boilerplate.
    </h2>
    <p class="mt-3 text-zinc-400 text-center">
      Each stage composes proven tools from across the AI coding harness ecosystem.
    </p>

    <div class="mt-14 flex flex-col md:flex-row md:gap-3 gap-3">
      {stages.map((stage, i) => (
        <>
          <div class="bg-[#15151D] border border-violet-400/20 px-4 py-3 rounded-lg flex flex-col items-center gap-1 md:flex-1 text-center">
            <span class="text-violet-400 text-xs">{stage.num}</span>
            <span class="text-zinc-200 text-sm font-medium">
              {stage.name}
              {stage.optional && <span class="text-zinc-500 text-xs ml-1">(opt)</span>}
            </span>
            <span class="text-zinc-500 text-xs">{stage.role}</span>
          </div>
          {i < stages.length - 1 && (
            <div class="hidden md:flex items-center text-violet-400/40 flex-shrink-0">
              <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </div>
          )}
        </>
      ))}
    </div>
  </div>
</section>
